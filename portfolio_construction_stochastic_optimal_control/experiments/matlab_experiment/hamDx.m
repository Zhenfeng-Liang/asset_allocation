function y = hamDx(x, p, mu, rho, sig, gamma) 
    y = round(1/(2*gamma)*p*cDx(x, rho, sig)*p + 1/gamma*p*aDx(x, mu)...
        +(1/gamma-1)*a(x, mu)*cInverse(x, rho, sig)*aDx(x, mu) + 0.5*(1/gamma-1)*a(x, mu)*cInverseDx(x, rho, sig)*a(x,mu),5);
    %y = 1/gamma * p * (sig*sig*x*p + mu);
end%%...